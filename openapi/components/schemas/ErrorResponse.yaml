type: object
required:
  - error
  - message
properties:
  error:
    type: string
    description: >
      HTTP response code.
    example: "403"
  message:
    type: string
    description: >
      Description of HTTP code.
    example: "Forbidden"
