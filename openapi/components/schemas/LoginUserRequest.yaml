type: object
required:
  - email
  - password
properties:
  email:
    type: string
    format: email
    description: >
      The email that the user account was registered with.
    example: craig@gmail.com
  password:
    type: string
    format: password
    description: >
      The users private password, used to log in.
    example: "@ssword741"
